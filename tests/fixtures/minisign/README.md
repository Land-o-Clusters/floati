# Minisign fixture bank

These keys authenticate test bytes only. The unencrypted secret key is public,
committed, and must never be used for a release or copied into `trust/`.

The fixture was generated with Minisign 0.12 using `-W`. Its trusted comment is
the exact compact JSON binding pinned by `tests/test_minisign_signing.py`.
