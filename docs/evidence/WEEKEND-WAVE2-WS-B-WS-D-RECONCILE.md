# DRAFT - Weekend Wave 2 WS-B / WS-D reconcile evidence

Date: 2026-08-28

Branch: `reconcile/weekend-20260828`

Declared starting main: `eb24ca58a2da69618f28d190f59fc7cab73131f5`

Merged lane tip: `4437639108772cc4539b97ae55232986e4644461`

Train commits before this evidence row:

- `69d240e` - merge the eleven WS-B / WS-D dark modules;
- `fdfd2ff` - commit exact previewed admin rows atomically; and
- `f3155f5` - activate the reconciled command surfaces and repair the seat fence.

## DRAFT - Activated rows

The train registered the eleven landed modules through one bounded
`register_admin_commands` seam:

1. B1 manifest-exact uninstall;
2. B2 nested workspace layout;
3. B3 node add/retire wizard;
4. B4 declared-root Harbor Chart;
5. B5 explicit read-only foreign-bus survey;
6. B6 provider/model switch receipts;
7. D1 typed shipped role templates;
8. D2 previewed role assignment;
9. D3 live boot/teardown projections;
10. D4 live node explanation; and
11. D5 metadata-only state-flush receipts.

All added visible help remains `DRAFT -` stamped. No README was edited. No
foreign bus was drained, acknowledged, registered, locked, or modified.

## DRAFT - Registry and compatibility receipts

- Preview adapter focused bank: 90/90 green before CLI activation.
- Activation/copy/manifest/source focused bank: 154/157 on the first run; the
  only reds were the expected generated COPY-LEDGER and frozen 125-to-132 asset
  train rebaselines. After mechanical regeneration, the focused failures were
  6/6 green.
- The first repository-wide discovery run executed 1,795 tests and found one
  compatibility error: the legacy `retire` artifact had lost its top-level
  `node_id` when workspace evidence was composed. The corrected artifact keeps
  the historical registry fields at top level and adds the typed `workspace`
  field. The exact RED plus admin regression slice then passed 8/8.
- The final repository-wide discovery run executed **1,795 tests in 190.383s,
  0 failures, 0 errors, exit 0**. Host subprocess probes printed their known
  `sandbox initialization failed: Operation not permitted` diagnostics and
  ResourceWarnings; neither changed unittest status.

## DRAFT - Draft-suite discovery receipt

Before wiring, ordinary discovery contained 1,762 tests and zero IDs from
window scheduling or night watch. The standalone gates were green at 12/12 and
19/19 respectively. The wrappers import the real draft assertions without
copying them, and two count/name pins make deleting either adapter visible.
The combined discoverable slice is **33/33 green**.

## DRAFT - Current-seat fence repair

Architect dispatch `msg-01a045ca3c75782b83a0b7f8ebb34da0` reported that the
common hook still hardcoded the retired `build lane` seat. The repair is:

- opt-in per worktree through `floati.seatFenceRoot` and
  `floati.seatFenceNode` worktree-local Git coordinates;
- absent coordinates bypass only the identity fence and still execute the
  manifest guard;
- partial coordinates refuse;
- the configured node must be active in the selected Floati registry through a
  physically read-only snapshot; and
- committer name/email must match the current active node exactly.

The active lane is configured as `build lane` against the declared
`~/.floati-bus/the fleet` root. A no-identity commit refused, the exact
registry-backed lane identity passed, and the 4/4 perturbation bank includes a
read-only-root proof that no lock file is created.

## DRAFT - Manifest and scrub closure

The bundle manifest was regenerated after every deployable edit and will be
regenerated once more as the last mechanical operation before commit.

Final manifest SHA-256:
`1a57ca0be221b4f3cc5e04d0762db2c842b9f04c2a104bccd6747f18b791e984`

Required source-tree scrub: `[]`, exit 0.

Required Git-history-note scrub: `[]`, exit 0.

Publication, activation, tenant migration, flip, and release remain outside
this evidence. the architect gates behind the train and was not awaited.
