# H — capability-matrix receipt-class re-round

**Seat:** grok · **Root:** puddle-fleet · **Date:** 2026-08-28
**Dispatch:** `msg-01a046b7cc3a7b529478ad2fa7d06606`
**Base:** `origin/main` `0911b3adf03bd96cd0e8b4b444cc12f5ad10bc47` (matrix already merged).

Law applied: a value cell cites what proves the capability.

## Diff

| column | before | after |
|---|---|---|
| work | 3× `adapter` citing `COPY-LEDGER.md` | 7× `adapter` citing C1–C7 (modules on main: `codex`, `claude`, `opencode`, `cursor`, `cline`, `grok_build`, `pi`). Desktop/chat/extension dash via C0-DELTA. herdr dashes via wave2-r3 (observation-only, not a worker adapter). t3/devin/antigravity dash via their C-rows (no module). |
| boot | Codex CLI `yes` cited the profile-repoint receipt | Every boot cell cites `WS-D3-NODE-LIFECYCLE-PROJECTION-WIRING.md`. `yes` only Codex CLI (D3 tests photograph `ManagedVerbShape(Codex, ~/.codex/bin/codex-fleet-bus)`). Other surfaces dash with that same citation. |
| managed_send | Codex CLI `yes` cited `WEEKEND_PROGRAM` (documents, does not prove) | Codex CLI `yes` cites `gate-wsb-b5-2026-08-27.md` (transport-test envelope `msg-01a0455838977b229394bcf297b43034`). Dashes cite `fleet-bus-profile-repoint-lane-puddle-2026-08-27.md` (Codex-only photographed profile). |

COPY-LEDGER and WEEKEND_PROGRAM are gone from these three columns.

## Unchanged

Bus, wake, auto_turnover (`—` until T2–T4, five `derivable: A`), compaction, provider notes, README-NEXT, tests, product source.

Hold order on ORGANIC ACCEPTANCE envelopes still stands. Envelope D was not opened.
