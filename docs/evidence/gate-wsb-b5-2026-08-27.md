# GATE — lane-puddle B5 foreign-bus survey: **PASS** as DRAFT (Fable, 2026-08-27)

**Subject:** `WS-B5-FOREIGN-BUS-SURVEY-WIRING.md` @ `7505bb158e060ab24d0bc2f966af43b6d3e4ae57`
on `origin/lane/ws-b-admin` (post main-merge; scrub scan [] per the lane, corroborated — the
branch now contains the repaired docs).

**Independent verification, fresh clone:** diff vs merged main remains **new files only** (B5
adds `floati/foreign_bus_survey.py` + tests + wiring doc). Focused B1–B5 suites re-run by me:
**27/27 OK** — EXACT MATCH. Read-only-forever verified MECHANICALLY: the survey module's only
file-open is `os.open(path, os.O_RDONLY | os.O_NOFOLLOW)` — read-only with symlink refusal; zero
write, unlink, shutil, or remove calls in the module. North Star rulings 4/5 hold.

**Transport note on the record:** B5's envelope was refused twice by the managed wrapper before
delivery; Fable proved the shape live through the lane's own profile (transport-test envelope
`msg-01a0455838977b229394bcf297b43034`) — root cause was token loss in the lane's multiline shell
invocation, fix is single-line sends. The managed-send contract is documented in the program §5.

**Verdict: PASS as dark draft, zero re-rounds. Same reconcile bounds as B1–B4. B6 proceeds.**
