# README SHOT LIST — every image is a real capture of a real fleet (Fable, 2026-08-28)

Companion to `docs/README-NEXT.md`. **Law: no shot is staged, mocked, or animated by hand — every
capture is played from a real ledger** (the hero GIF set the precedent). Capture tooling exists:
`scripts/capture-demo-assets.py`, `scripts/capture-wall.py`. A shot cannot be taken before its
feature lands; the LANDS column sequences the captures behind the train. Light/dark variants per
the committed wall discipline. Executed by a mechanical seat (luna criteria) or lane-floati at
train's end; Fable reviews every frame before it enters the README.

| slot | shows | capture | lands with |
|---|---|---|---|
| `hero-three-fault-replay` | worker kill · sequencer kill · reboot · resume | **EXISTS** (`docs/demo/`) | shipped |
| `harbor-board` | three lamps distinct: liveness/authority/lock on a live 3-node fleet | board capture on a scratch fleet mid-run | main today |
| `flight-recorder-replay` | `log --replay --speed 4` of a completed orchestration | terminal capture, standard+plain | main today |
| `doctor-delivery-health` | per-node scoreboard with one aged-pending RED + `--probe` PASS/DEAF | scratch fleet with one deliberately deaf node | Car 3 |
| `onboard-wizard` | `node add` full flow incl. records-preview-before-write beat | interactive capture | WS-B B3 |
| `harbor-chart-multibus` | ASCII chart of 2+ declared roots, architect + downstream visible | scratch twin fleets | WS-B B4 |
| `survey` (candidate) | foreign-bus report incl. a foreign waiter bound to a workspace | fixture root, never a live foreign bus | WS-B B5 |
| `wake-ping` (candidate) | envelope sent → lane wakes sub-deadline, wake receipt shown | live capture during P0 verification | WS-A P0 |
| `uninstall-dry-run` (candidate) | `--dry-run` full removal set + never-prune-foreign line | scratch install | WS-B B1 |

**SEQUENCING RULED (owner + Fable, 2026-08-28): CAPTURES ARE THE LAST MOVER before the README
swap.** They freeze only behind ① the reconcile (surfaces wired), ② the Fable copy restamp (no
DRAFT copy may appear in a permanent frame), and ③ **gauntlet green** — a gauntlet-caught defect
whose fix changes any rendered surface would invalidate its captures, so no frame is taken while
the armor is still finding holes. A capture is a receipt of a surface; you do not receipt a
surface that can still change.

Candidates ship only if the frame is genuinely good — the README needs the best six, not all nine.
Rejected forever: fabricated terminals, sped-up typing, synthetic data styled as real.
